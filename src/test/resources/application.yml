spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/fashionteer-community-test
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: root
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update